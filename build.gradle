plugins {
    id 'java'
}

group = 'net.quepierts.animata4j'
version = '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

tasks.named('build') {
    dependsOn(':codegen:build')
    dependsOn(':core:build')
}